# Claude Code 運用ガイド

このリポジトリは macOS 上の pane を安全に管理するための CLI である（backend は iTerm2 と tmux の2つ）。Claude Code のセッション間メッセージ自体は実装しない。オーケストレーター Skill が `ListAgents` と `SendMessage` を使い、本 CLI は worker pane の lifecycle だけを担当する。

## 変更時の不変条件

- 対応対象は macOS と Linux である。backend は iTerm2（macOS 専用）と tmux（macOS/Linux 両対応）の2つ、`infrastructure/terminal/iterm_backend.py` / `infrastructure/terminal/tmux_backend.py` のように `run(request) -> dict` の同じ操作インターフェース（`spawn` / `close`、キーは `pane_ref` / `window_ref` / `anchor_pane_ref` で統一）を持つ独立モジュールとして追加する。どちらの backend を使うかは registry に保存せず、`infrastructure/terminal/resolver.py` の `detect_backend()`（spawn 時、環境変数から選択）と `infer_backend()`（close 時、`pane_ref` の形— tmux は `%<digits>`、iTerm2 は UUID — から判定）が都度計算する（backend の選択は環境やその時の使い方で変わりうるため、固定のフィールドとしては記録しない）。`infer_backend()` への依存（spawn 時の不一致検知、close 時の dispatch など）は load-bearing（削除不可 — close は原理的に「どちらの backend か」を何かの方法で判定せねばならず、これが最も安上がり）。`sukuna-cli tree` は backend を表示しない。**`$TMUX`/`$ITERM_SESSION_ID` が両方立っている場合に限り、`detect_backend()` は `infrastructure/settings.py` の `preferred_backend` 設定（`sukuna-cli init` の対話プロンプトが書く、値は `"tmux"`/`"iterm2"`）を内部で直接読んでタイブレークする。`detect_backend()` のシグネチャに引数は足さない（resolver.py が設定を直接読む設計）。片方しか立っていない場合はその backend を選び、両方無い場合は `ValidationError`。設定が無い場合は無条件で tmux を優先する。
- task 本文を shell command に埋め込まない。worker は `claude -n <session-name>` だけで起動し、task は cross-session message で渡す。
- registry にない pane、`managed: false` の worker、`accepted` 以外の worker は絶対に close しない。
- backend 呼び出しの失敗時は pane を残し、worker を `failed` に遷移させる。
- registry の更新は lock と atomic rename を維持する。
- lifecycle や close 判定を変更したら pytest を追加・更新する。
- **anchor / `worker_count` は「実際に pane を持つ worker」だけを数える。** `existing_workers()` は `CLOSED` 以外を全部拾うため、pane 作成前に `FAILED` になった worker（`pane_ref is None`）が混じる。これをそのまま anchor にすると `None` を anchor として backend に渡してしまい、次の spawn が壊れる。`domain/service/spawn_placement.py` の `alive_pane_holders()` が `pane_ref is not None かつ state is not CLOSED` で絞り込み、配置決定（下記「配置規則」参照）と `worker_count` はこちらを使う。**`ordinal`＝命名用の連番はこれとは別集計。** `domain/service/spawn_placement.py` の `next_ordinal()` は、このセッションに属する registry 上の**全レコード数（state・managed を問わず CLOSED も含む）+1** を数える — `existing`/`alive_pane_holders()` のどちらとも別の集計基準で、決して再利用されない連番になる。CLOSED を母数から除くと、close 直後の再 spawn が同じ ordinal・同じ決定的名前（`worker_name()`）を再生成し、registry に残る自分自身の CLOSED レコードと衝突して `Registry.add()` が `already exists` で拒否し、spawn が恒久的に失敗する。**「全レコード数」の集計だけでは30日retention（下記「retention」節）と両立しない — CLOSED/FAILED が削除された直後は件数が減って ordinal が巻き戻りうる。** そのため `next_ordinal()` は「レコード数ベースの値」と「`RegistryDocument.ordinal_high_water_marks`（`parent_session_id` ごとの永続フロア、`Registry.add()` の `claim_ordinal` で成功spawnのたびに前進）」の大きい方を返す。詳細は下記「retention」節。
- **同一 orchestrator セッション内での backend 混在は spawn 前に拒否する。** `usecase/spawn.py` の `spawn()` が既存 worker（同じ `parent_session_id`）の最後の pane の形と、今回 `detect_backend()` が返した値を比べ、食い違えば pane 操作を一切試みずに `ValidationError` を返す（`cli.py` の `handle_spawn` は引数をそのまま usecase 層へ渡すだけの薄いラッパーで、この判定自体は持たない(role/parent_worker_name の validation も usecase/spawn.py 側にある)）。この検査が無いと、iTerm2 worker の session ID を tmux の anchor として渡すような不整合な呼び出しがそのまま backend まで到達し、原因の分かりにくいエラーになる。
- **`WorkerRecord` のフィールド追加・改名は on-disk registry の後方互換を壊す（`domain/mapper/registry_mapper.py` の `record_from_dict()` が `cls(**value)` の素通しのため）。** 互換を壊す変更は、pip 配布先の実ユーザー環境で既存の `~/Library/Application Support/sukuna/registry.json` を全損させうる。旧キー名の読み替えは `record_from_dict()` が呼ぶ `_migrate_legacy_fields()` に実装されている（対応表はその docstring と実装が正準）— 将来フィールドを変える時はここに読み替えを足すか、`schema_version`（現在 `3` — `RegistryDocument`（`WorkerRecord` 単位ではなく registry 全体）のトップレベルフィールド `ordinal_high_water_marks`/`last_swept_at` に対応する値。両フィールドとも安全なデフォルト（`{}`/`None`）を持つため `_migrate_legacy_fields()` 側の読み替えは不要 — pydantic のデフォルト解決だけで v1/v2 registry もそのまま読める。バージョン番号は「このフィールドを知らない旧 sukuna が v3 registry を読んで黙って握りつぶす」ことを防ぐガード）を使った migration を検討すること。
- **`name` が唯一の識別子。** `WorkerRecord.name` が registry の主キーであり、`--worker` フラグの値であり、`claude -n`/`ListAgents`/`SendMessage`/`claude --resume` が使う同じ名前でもある。`worker_id` のような sukuna 独自のUUIDや、呼び出し側が発明する `run_id` は存在しない — 「同じ実行に属する worker 群のグルーピング」は `parent_session_id`（`$CLAUDE_CODE_SESSION_ID`、呼び出し側が意識せず自動で付く）が担う。`existing_workers()`/`alive_pane_holders()` は `parent_session_id` でグルーピングしており、`sukuna-cli tree` のグルーピングキーと完全に同じ概念である。
- **registry のフィールドに `role` は無く、`goal` と `updated_at` を持つ。** `role`（spawn の JSON 入力キー）は `worker_name()` が name 文字列（`ccw-...-<role>-N`）に埋め込むためだけに使い、`ordinal`（連番）は role ごとではなく session 全体の連番。その母数は `next_ordinal()` が数える session 内の全レコード数（CLOSED 含む）——詳細は上記「anchor / `worker_count`」の項目参照。`goal`（spawn の JSON 入力キー。自由記述、任意）は「何のための worker か」を後から見返すためのもので、**`worker_command()` には一切渡さない**（`claude` に `--goal` という引数は存在しないため。テストは `test_spawn_stores_goal_but_never_forwards_it_to_the_command` で担保）。`created_at` は持たず、`updated_at` が `WorkerRecord.transition_to()` の呼ばれるたび（`spawn` の READY 遷移、`state`、`accept`、`close` すべて）に更新される（必要なのは作成時刻ではなく最終更新日時、という整理）。
- **生存する子(`alive_pane_holders()` と同じ定義: pane を持ち、CLOSED でない — FAILED で pane が残る子も含む)を持つ worker は close を拒否する。** `usecase/close.py` が `may_close` 検査の直後・pane 破壊の前に registry から子を導出し、非空なら `ValidationError`(該当子の名前を列挙)を返す。孫の掃除は子自身に行わせる — 孫が生きている限り子の close も同じガードで拒否される連鎖で、ボトムアップを強制する。pane が実際には消えている子がブロックする場合は `sukuna reconcile` を実行する — reconcile は pane 消滅を確認した worker を FAILED に遷移させ **pane_ref もクリアする**ため、ブロックが解消される。
- **単一パッケージ・2 entry point 構成。** `pyproject.toml` の `[project.scripts]` に `sukuna = "sukuna.cli:main"`（エージェント向け、`spawn`/`inspect`/`state`/`accept`/`close`/`respawn`/`capture`/`reconcile`/`focus` の9コマンド）と `sukuna-cli = "sukuna.cli_human:main_cli"`（人間向け、`doctor`/`tree`/`init`）の2エントリーポイントを持つ。分割基準: オーケストレーター手順(本ファイル1-7)・dev-loop skillの手順に呼び出しが明記されているか、出力契約がJSON(機械可読)かテキスト/対話(人間可読)か — の両方で判定し、該当しない3コマンドのみが`sukuna-cli`側にある(`inspect`はJSON出力・`--registry`テスト整合性からエージェント側)。共通処理(`emit()`/registry解決/`main()`のエラーハンドリング)は `cli_shared.py` の `emit()`/`registry_from_args()`/`run()` に集約し、`cli.py`(agent向け実体)と `cli_human.py`(human向け実体、`doctor`/`tree`/`init`のハンドラと対話プロンプト群を持つ)の両方から呼ぶ。`sukuna-cli init` が導入する hook は Node.js の `execFileSync("sukuna", [...])` でバイナリ名を実行時に PATH 解決するだけなので、この分割方式による影響は受けない(`sukuna`/`sukuna-cli` は同一パッケージの同時インストールが保証されるため)。
- **CLOSED/FAILED レコードは30日retentionで退役する。** `infrastructure/settings.py` の `retention_days`（デフォルト `DEFAULT_RETENTION_DAYS = 30`、`sukuna-cli init` で設定可、他の設定項目に対する対話順序は不問）を基準に、`updated_at` から `retention_days` 日以上経過した CLOSED/FAILED レコードを削除する — registry の唯一の削除経路。TIMED_OUT は対象外（`domain/service/retention.py` の `_RETIRABLE_STATES` は `TERMINAL_STATES` と意図的に別集合）。FAILED は追加で `pane_ref is None` を要求する（`reconcile_filter.py` の `stale_pane_candidates()` が示す通り、FAILED はpaneがまだ生きている・調査中のことがあるため）。掃引そのものは `domain/service/retention.py` の `sweep_stale_records(path, records, *, retention_days, now)`（ストレージ非依存の純粋関数、`(path, survivors, purged)` を返す）が判定し、`infrastructure/registry.py` の `Registry.purge(*, retention_days, now, force=False)` が実際の読み書きを担う。実行タイミングは二本立て: `usecase/spawn.py` の `spawn_many()` が呼び出しのたびに `force=False` で自動実行（`sweep_is_due()` により UTC暦日1回に間引き）、`sukuna reconcile --prune` が `force=True` で明示実行（間引きをバイパスし、`retention_days` を短くした直後にも即座に効く）。**ordinal high-water-mark（上記「anchor / `worker_count`」節参照）の永続化はこの retention の直接の前提** — `Registry.purge()` は削除前に、削除対象レコードが属していた各 `parent_session_id` のフロアを `domain/service/retention.py` の `seed_ordinal_floors_before_purge()` で `(削除前のそのセッションの全レコード数 + 1)` まで引き上げてから削除する（v1/v2registryでHWM未設定でも安全に機能する設計）。`build_tree()`（`sukuna-cli tree`）は retention で親だけが消えたケースも既存の `"(orphaned: parent '<name>' not found)"` バケットでそのまま扱う（新規コードパス不要 — 手動closeで親だけ消えたケースと区別しない）。

## `parent_session_id` / `sukuna-cli tree`

- `spawn` は呼び出し元の `$CLAUDE_CODE_SESSION_ID`（Claude Code が全セッションに自動で設定する環境変数）を worker の `parent_session_id` として記録する。`sukuna-cli tree` はこれで registry を親セッションごとにグルーピングして返す。
- **`parent_session_id` 単独では1階層より深くは繋がらない。** iTerm2 の custom profile も tmux の新規 pane も、`sukuna` プロセス自身の環境を継承せずに `claude` を起動するため、worker 自身が受け取る `$CLAUDE_CODE_SESSION_ID` は sukuna から観測できない別の値になる。`parent_worker`（spawn の stdin JSON 配列の要素キー）を指定せずに孫 worker を spawn した場合、孫の `parent_session_id` はその worker 自身の（sukuna が知らない）session ID になり、`tree` 上では親と繋がらない別のルートグループとして現れる。
- **`parent_worker`（spawn の stdin JSON 配列の要素キー）で任意深さの多段親子関係を追える。** `WorkerRecord.parent_worker_name`(default `None`)に、呼び出し元 worker 自身の `name` を明示的に渡す。渡された名前は検証(worker 名として妥当な形式かどうか)のみ行われ、registry への実在チェックはしない — 呼び出し時点で親がまだ registry に無い、または既に close されているケースを許容するため。`sukuna-cli tree` はこの値がある worker を、その名前を持つ worker ノードの子として再帰的にネストする。指定名が registry に実在しない worker は `"(orphaned: parent '<name>' not found)"` に、どのルートからも(root/orphan いずれの経路からも)到達できない独立した循環参照(手動で registry を書き換えた場合などにのみ起こりうる)は `"(cycle: unreachable from any root)"` にまとめられる。**sukuna 自身は「子が親の名前を知っている」ことを保証しない** — bootstrap message でその名前を伝える運用は sukuna の外側(オーケストレーター Skill)の責務。
- `parent_session_id` が `None` の worker（`$CLAUDE_CODE_SESSION_ID` が未設定な環境、手動テストなど）は `sukuna-cli tree` の出力で `"(unknown parent)"` にまとめられる。

## オーケストレーターとしての手順

1. `ListAgents` で到達可能な live session を調べる。
2. 同じ repository、worktree、goal に一致する managed worker があれば再利用する（`role` は `name` の生成にしか使われず、registry のフィールドではない）。名前だけで一致させない。
3. worker がなければ、orchestrator pane（iTerm2 backend ならその iTerm2 pane、tmux backend ならその tmux pane）を選択した状態で `sukuna spawn` を呼ぶ。backend は指定しない（`$TMUX` があれば tmux、なければ `$ITERM_SESSION_ID` があれば iTerm2、で自動選択）。**`spawn` は argv フラグを取らない — stdin に JSON 配列を渡す。各要素は `role`/`repo`/`worktree`（必須）と `goal`/`parent_worker`/`model`（任意。`model` は `claude --model <値>` として起動コマンドに付与される — 指定時のみ付与、未指定時はデフォルトモデルで起動。呼び出し元プロセスの環境変数に `$ANTHROPIC_API_KEY` があれば、指定された `model` を Anthropic の models-list API と突き合わせる opt-in 検証が入る — ローカルキャッシュにヒットすればネットワーク不要、ミスした場合のみ1バッチにつき最大1回まとめて再取得し、それでも見つからない・再取得自体が失敗した場合はその要素が failed になる。空文字・空白のみの `model` は `$ANTHROPIC_API_KEY` の有無に関わらず preflight で拒否される（`model` の字句検証はこの空文字・空白チェックのみで、それ以外の字句検証は実装しない）。それ以外の値は `$ANTHROPIC_API_KEY` が無い場合は無検証で素通しする）をキーに持つオブジェクト。1件だけ spawn する場合も要素数1の配列で呼ぶ。複数 worker が必要な場合は1回の呼び出しにまとめてバッチ化し、呼び出し回数（＝トークンコスト）を抑える。**
4. JSON 出力は `{"succeeded": [{"index": N, "worker": {...}}], "failed": [{"index": N, "spec": {...}, "error": {...}}]}`。`succeeded[].worker.name` を取得する（`--worker` フラグや `SendMessage`/`ListAgents` の宛先はすべてこの `name`）。worker が agent list に現れるまで上限付きで確認する。**要素の JSON 構造・必須キー欠落・`repo`/`worktree` の空文字（エラー文言 `repo and worktree must be non-empty paths`、`domain/service/spawn_preflight.py`）・`repo`/`worktree` が存在しない・`role`/`parent_worker` の形式不正・`model` の空文字・空白のみのいずれかが1件でもあれば、バッチ全体が拒否される（pane 操作ゼロ、プロセス終了コード `2`）。それを通過した後の backend 呼び出し失敗などは要素単位の best-effort（`failed` に該当要素だけが入り、他の要素は spawn される）— この場合は `failed` が空でなければ終了コードが非ゼロになる（`2` とは別の値）。**
5. 到達可能になった worker だけへ `SendMessage` で bootstrap task を送る。送信後に `sukuna state --state busy` を呼ぶ。`reported` の worker へレビュー指摘の反映・修正など追加作業を送る場合も、`SendMessage` の後に `sukuna state --state busy` を呼ぶ（`REPORTED → BUSY` 遷移は許可されている）。次の報告を受けたら手順6と同様に `reported` に戻す。
6. worker の報告を受けたら `reported` にする。成果と検証結果を確認できた場合だけ `sukuna accept` を呼ぶ。
7. `accepted` になった managed worker にだけ `sukuna close` を呼ぶ。**生存する子を持つ worker の close は sukuna 自身が拒否する — 孫を持つ worker を畳むときは、必ず孫→子→親の順(ボトムアップ)で `sukuna close` を呼ぶこと。子の close が「生存する孫がいる」理由で拒否されたら、先にその孫を close してから子を再試行する。**

`blocked`、`failed`、`timed_out`、message が hold / refuse された worker は close しない。pane を残し、人間が確認できる状態にする。

## worker への bootstrap message

短く、以下を含める。

- worker 名（`--worker` に渡した名前）
- task の範囲、対象 worktree、読むべき設計や指示
- 書き込み可否と必要な検証
- 完了報告の形式

完了報告には最低限、`STATUS`、`SUMMARY`、`CHANGED`、`VERIFY`、`HANDOFF` を求める。メッセージで権限の迂回や設定変更を要求しない。

## iTerm2 API の既知の罠

`spawn` が失敗したときは、まず `doctor` や registry の中身より前に次を疑う。

- **`it2run` は fire-and-forget。** `it2run <script> <request>` はスクリプトを iTerm2 本体プロセスへ渡した時点で即 exit 0 する。実際の Python 実行は非同期に始まり、数百ms〜数十秒後に走る。したがって `subprocess.run(it2run, ...)` の戻り時点で `result_path` が存在しなくても失敗と決めつけない（`infrastructure/terminal/iterm_backend.py` は `result_path` の出現を `self.timeout` までポーリングする実装にしてある — これを「1回チェックして終わり」に戻さないこと）。
- **実際のエラーは iTerm2 の Script Console にしか出ない。** シェルの stdout/stderr にも `log show`（macOS 統合ログ）にも出ない。CLI が `"no result returned"` としか言わない・原因不明のときは、ユーザーに iTerm2 メニュー → Scripts → Manage → Console... の内容を貼ってもらうしかない。
- **Custom Command はシェル演算子を解釈しない。** iTerm2 profile の `LocalWriteOnlyProfile.set_command()` は文字列をそのまま exec する（`sh -c` 経由ではない）。`cd X && exec claude -n name` のような文字列を渡すと `cd` という実行ファイルを探して即失敗し、iTerm2 側は "A session ended very soon after starting" という**GUIダイアログ**を出す（これがモーダルで、開いたままだと以降の `it2run` 呼び出し自体がブロックされ "no result returned" を連発する — 原因不明のエラーが続いたら、まずこのダイアログが開いていないか確認する）。`worker_command()` は `<解決したログインシェル> -lc '<cd && exec ...>'` で明示的にラップしてこれを回避する（`/bin/zsh` 決め打ちではなく、`infrastructure/terminal/resolver.py` の `resolve_login_shell()` が `$SHELL`（known-safe＝zsh/bash/dash/shの場合のみ最優先）→ `zsh`→`bash`→`/bin/sh` の順で解決する）。
- **`async_split_pane` はサーバ側成功後にクライアント側の session 登録が遅延することがある。** iTerm2 の RPC は "OK" を返しても、ローカルの `App` モデルが新セッションを認識する通知が遅れ、`SplitPaneException: No such session <id>` が飛ぶことがある。`infrastructure/terminal/iterm_script.py` の `resolve_new_session()` がこの id を拾って `app` を再取得しながら再ポーリングする。
- **`EnableAPIServer` は Preferences で有効化しても、直後の呼び出しはまだ失敗しうる。** 再起動なしで有効化できることもあるが、確実ではない。失敗が続く場合は iTerm2 の再起動を提案する（ただし今のセッション自身がそのiTerm2ペイン内で動いている場合、再起動でセッションが切れるリスクをユーザーに伝えてから行う）。
- **anchor 未指定時は現在の "current session" 解決に頼らない。** `current_session(app)`（= `app.current_window.current_tab.current_session`）の解決は不安定なため、同一 run 内に既存 worker がない場合の anchor は、`$ITERM_SESSION_ID` 環境変数（`<window><tab><pane>:<uuid>` 形式、`:` 以降が bare UUID）から取る `orchestrator_pane_ref("iterm2")` を使う — オーケストレーター自身のペインの ID を使うのが本来の意図に合う（tmux backend は `$TMUX_PANE` をそのまま使えるので同種の不安定さ自体がない）。

## tmux backend の使い方

`$TMUX` が設定された環境で `spawn` を呼ぶと自動的に tmux backend が選ばれる（`infrastructure/terminal/resolver.py` の `detect_backend()`、tmux が iTerm2 より優先）。使う前提と手順:

- **オーケストレーター自身（この CLI を呼ぶ Claude Code セッション）が tmux セッション内で動いている必要がある。** anchor は `$TMUX_PANE`（tmux がペインごとに自動で設定する環境変数、パースなしでそのまま使える）から取る。`$TMUX` が空なら tmux backend は選ばれない（`$ITERM_SESSION_ID` があれば iTerm2 backend にフォールバック、どちらも無ければ `spawn` 自体が `ValidationError` で拒否する）— ユーザーに「`tmux new -s <name>` してその中で `claude` を起動し直してほしい」と依頼する。sukuna 自身の spawn では tmux セッションは作れない（オーケストレーターを外から tmux に押し込む操作はできないため）。
- `sukuna-cli doctor` は `tmux_available` で `tmux` コマンドの有無を報告する。iTerm2 側の `it2run_available` と独立。
- **iTerm2 backend にある罠（上記セクション参照）は tmux backend には存在しない。** `tmux split-window -P -F ...` は同期的に新しい pane/window ID を返すため、非同期ポーリングも Script Console 頼みのデバッグも不要。`worker_command()` の `<解決したログインシェル> -lc '...'` ラップ（上記「iTerm2 API の既知の罠」節参照）は tmux 経由でもそのまま正しく動く（tmux も渡された文字列をシェル経由で実行するため、二重ラップになるだけで害はない）。
- **配置規則は registry 主導の再帰。** 実ジオメトリ(tmux pane の座標、iTerm2 の split tree)は一切読まない。親 X（spawn の JSON 入力キー `parent_worker` で指定した worker、未指定ならオーケストレーター自身）の生存子（registry 上で `parent_worker_name` が X を指し、pane を持ち、CLOSED でない worker、`sukuna-cli tree` が使う親子関係と同じ）を registry から引く: 生存子が0なら X 自身の pane を左右分割（`-h`）して右に置く、1体以上なら spawn 順で最後の生存子の pane を上下分割（`-v`）して下に置く。新しい window は作らない（葉数の閾値・分割方向の交互ロジックは持たない）。anchor に選んだ pane は分割前に1回 backend へ実在確認する — 親名が解決できる場合(typo・close 済み・FAILED でそもそも解決できない、のいずれでもない場合)に限りその anchor が消えていたときだけ、オーケストレーター直下（ルート）へ1回だけフォールバックして再確認する。親名がそもそも解決できない場合は最初からルート扱いになる。ルート自身の anchor（オーケストレーター自身の pane を含む）が消えていた場合はそれ以上のフォールバック先がなく、spawn 自体を失敗させる（`ValidationError`、worker は pane を持たず `FAILED`）。同じ親から worker を積み上げると、その親の右側に伸びた列の下にどんどん積み重なる見た目になる。
- **spawn 成功後、同じ列(親 X の生存子縦積み列)のメンバーが2体以上になった場合、および close 後に列へ残ったメンバーが2体以上の場合は高さを均等化する。** 対象は親 X 自身を含まない列メンバーのみ。目標高さは列メンバー自身の現在の高さの合計(tmux は `display-message` のスカラー読み、iTerm2 は `preferred_size` のローカル読み — 「誰が列か」は registry で確定しており、実ジオメトリを走査するレイアウト読み取りは行わない)。tmux は先頭 N-1 メンバーへ `resize-pane -y`(末尾は自動吸収)、iTerm2 は `preferred_size` 設定+`Tab.async_update_layout()` 1回。**均等化の失敗は spawn/close を失敗させず、JSON 出力の `equalize_warning` で通知する**(worker の状態・registry は影響を受けない)。比率指定は将来の直交事項で未実装。
- `close` は `pane_ref` の形（`%<digits>`）から `infrastructure/terminal/resolver.py` の `infer_backend()` が `tmux` と判定して自動的に `TmuxBackend` を選ぶ。呼び出し側で backend を指定する引数はそもそも存在しない。

## 開発・検証

変更後は次を実行する。

```text
PYTHONPATH=src python -m pytest
python -m build
ruff check .
ruff format --check .
python -m mypy src/ tests/
(cd tui && npm run lint && npm run typecheck && npm test)
```

**`tui/`（`sukuna-cli tree --tui`のInk製TUI）のJS側品質ゲートは、Python側のruff/mypy/pytestと同じrequired強度で扱う。** `(cd tui && npm run lint && npm run typecheck && npm test)`が上記コマンド列に含まれる。`python -m build`は`tui/`のビルドを自動では行わない（setuptoolsはpackage-dataのglobが1件もマッチしなくてもエラーにしないため）— `_tui_bundle/tree-tui.mjs`を実際にwheelへ同梱するには、`python -m build`より前に`scripts/build_tui.sh`（`cd tui && npm ci && npm run build`のあと`tui/dist/tree-tui.mjs`を`src/sukuna/_tui_bundle/tree-tui.mjs`へコピーする）を実行する必要がある（iterm2 extraの既存の注記と同じ性質の注意書き）。`scripts/build_tui.sh`を実行しないまま`python -m build`したwheelは`--tui`実行時に「bundle not found」の`BackendError`になる。**`tui/`ソース一式（`src/sukuna/_tui_source/`としての同梱）は`setup.py`の`TuiSourceBuildPy`（`build_py`のビルド時フック）と`MANIFEST.in`が担う** — GitHubからの`pip install git+...`直接インストールでも`sukuna-cli init`がその場でTUIバンドルをビルドできるようにするための仕組みで、working tree側に生成物は残らない（`.gitignore`への追加は不要）。`python -m build`後は生成wheelを`unzip -l`で検査し、`sukuna/_tui_source/**`（`package-lock.json`を含む）が同梱されていることを確認する。

**上記を実行する仮想環境には `iterm2` が必ず入っている必要がある。** `iterm2` パッケージが無い環境では、`ignore_missing_imports = true` により `iterm_script.py` の `iterm2` import が `Any` 型扱いとなり、`disallow_untyped_calls`（`sukuna.*` 向け strict override）が発火しないため、`profile = iterm2.LocalWriteOnlyProfile()  # type: ignore[no-untyped-call]` の `type: ignore` コメントが「不要」と誤判定され、`mypy` が `Unused "type: ignore" comment [unused-ignore]` を偽陽性として報告する（コード側の問題ではなく、`iterm2>=2.7` を実際にインストールすればこのエラーは消える）。mypy のエラー件数を測定・比較する時も `iterm2` を含む環境で行う（この extra を欠いた `.venv` 等では数値が食い違って見えることがある）。`dev` extra（`pip install -e ".[dev]"`）は `build` パッケージと `sukuna[iterm]`（＝`iterm2`）を内包する — `pip install -e ".[dev]"` だけで `pytest` / `python -m build` / `ruff` / `mypy` の4コマンドが揃って green になる。`.[iterm]` 単体で（dev 抜きで）インストールする場合は `iterm2` のみが入る。

lint/format は Ruff。`pyproject.toml` の `[tool.ruff]` は `include = ["src/**/*.py", "tests/**/*.py"]` のみ指定する（ルールセットへの追加は下記 `extend-select` の項）。`ignore`/`per-file-ignores`/`# noqa` は使わず、violation はコード側の修正で解消する（例: `RUF012`（テストのスパイ/レコーディングパターンのクラス属性）は `ClassVar[...]` アノテーション、`BLE001`（`iterm_script.py` のトップレベル catch-all、既知の罠セクション参照）は `logger.exception()` の追加、`DTZ007`（naive なローカル時刻文字列の再パース）は `datetime.strptime` から `datetime.fromisoformat` への置き換え、で解消している)。フォーマットは `ruff format .` の一括適用のみで、行長など個別設定はしていない。

**デフォルト有効ルールセットに `[tool.ruff.lint]` の `extend-select`（`select` ではない — 既存のデフォルト有効ルールセットは変えない）で `PLR0912`（too-many-branches）/`PLR0913`（too-many-arguments）/`PLR0915`（too-many-statements）/`C901`（complex-structure）の4つを追加している。** 閾値のうち max-args=5 / max-statements=50 / C901 complexity=10 は Pylint 既定（max-branches・max-locals は次項の通り引き締めている）。これらの violation もリファクタで是正する（引数過多はパラメータオブジェクト化 — `dataclass` または `TypedDict`+`Unpack`(PEP 692) — で、分岐過多・複雑度過多は関数分割で解消する）。

**`[tool.ruff.lint]` に `preview = true` を置き、`extend-select` に `PLR0914`（too-many-locals）を加えている（`PLR0914` は preview 機能のため）。** `preview` は必ず `[tool.ruff.lint]` にスコープする — トップレベル `[tool.ruff]` に置くと `ruff format` の preview 挙動まで波及するのに対し、`lint.preview` に限定すれば `ruff check`/`ruff format` いずれも新規の副作用なしに `PLR0914` の selection だけを解禁できる。`[tool.ruff.lint.pylint]` は `max-locals = 8`・`max-branches = 6`（`PLR0913`/`C901` は既定のまま）。この閾値はレビュー基準そのもの（max-locals=5/max-branches=4）ではなく、是正コスト対効果との兼ね合いで緩めた値。5/4 への再引き締めは将来の別カードで検討する。これらの violation もリファクタで是正する（関数分割によるローカル変数削減、`if/elif` 連鎖の辞書ディスパッチ化による分岐削減 — 例: `TmuxBackend.run()`／`cli.py` の `parser()` はサブコマンド登録を1関数1個ずつに分割している）。

型チェックは mypy。`pyproject.toml` の `[tool.mypy]` は `python_version = "3.11"` と `ignore_missing_imports = true`（`tests/` 向けのbasic相当を兼ねる、トップレベルに配置）のみを持ち、`[[tool.mypy.overrides]]`(`module = "sukuna.*"`)にstrict相当の個別フラグ12個（`disallow_any_generics`/`disallow_subclassing_any`/`disallow_untyped_calls`/`disallow_untyped_defs`/`disallow_incomplete_defs`/`check_untyped_defs`/`disallow_untyped_decorators`/`warn_unused_ignores`/`warn_return_any`/`implicit_reexport = false`/`strict_equality`/`extra_checks`）を明示列挙して `src/`(=`sukuna`パッケージ) だけを strict 相当にする、という混在 strictness（`tests/` まで strict にすると是正量が大きすぎるため、`tests/` は basic・`src/` のみ strict）。**per-module override で `strict = True` という省略形は使わない** — mypy 2.3.1 には、この省略形が本来スコープされないはずのモジュール（`tests/`）にまで個別フラグを漏らすバグ相当の挙動があるため（個別フラグの明示列挙で回避する）。`implicit_reexport = false` の影響で、`domain` 層の関数を `infrastructure` 層が再エクスポートする箇所（例: `infrastructure/settings.py` の `validate_active_pane_width`）はモジュールの `__all__` に明示列挙して再エクスポートする（`from X import Y as Y` 形式は Ruff の `PLC0414`(冗長な import alias) に引っかかり `ignore` なしのRuff方針と衝突するため不採用）。violation は `# type: ignore` で握りつぶさず修正で解消する（Ruff の `# noqa` と同じ方針）。

エディタ上の型ヒント(Pyright/Pylance)は mypy とは別ツール・別設定であり、CIのrequired stepには含めない。`pyproject.toml` の `[tool.pyright]`(`pythonVersion = "3.11"`、`include = ["src", "tests"]`)は`[tool.mypy]`/`[tool.ruff]`と同じ最小限のスコープ指定に揃えている。`iterm_script.py`はiterm2 SDK自体が`__init__.py`で`__all__`無しのワイルドカードexportをしているため、pyrightの`reportPrivateImportUsage`/`reportAttributeAccessIssue`をそのファイル冒頭の`# pyright: reportPrivateImportUsage=false, reportAttributeAccessIssue=false`コメントでのみ無効化している(mypyの`iterm2.*` overrideと同じ意図をファイル単位で再現、プロジェクト全体には広げない)。

pane を実際に操作する `spawn` / `close` は環境を変更する。iTerm2 backend・tmux backend いずれのテストも `subprocess.run` を `monkeypatch` してロジックを検証する（実ターミナルへ送信するテストは flaky になりやすいため）。tmux backend 側は `tests/test_tmux_backend.py`（`TmuxBackend` の spawn/close/verify/capture ロジックと `_run()` 自身の returncode 非ゼロ・timeout → `BackendError` 変換経路をモックで検証）と `tests/test_tmux_smoke.py`（実 tmux バイナリへの疎通だけを確認する最小限の smoke test、spawn→close 1往復、ロジックのアサーションは持たない）の2ファイル構成 — 後者は mock せず実際の `tmux` CLI に対して行う（`tmux` 未インストール環境では自動 skip される）。