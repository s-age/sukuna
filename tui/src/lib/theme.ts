// Shared color tokens: the focused pane's border reuses the same
// "active" color `WorkerList` already uses for the selected row, so
// the two stay in sync from one definition instead of two copies of
// the literal drifting apart.
export const ACTIVE_COLOR = "cyan";
